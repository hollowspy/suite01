/*
  Ecrire une fonction "sortNumbers" qui va trier un tableau de nombres.
  Prototype:
      void sortNumbers(arr);
  Fonctions interdites:
      Array.sort
*/

//  écrire votre code sous ce commentaire

const sortNumbers = (_array) => {
    for (var i = 0; i < array.length; i++) {
        while (array[i] >) {

        }
    }
}

/*
  Test 1
  Résultat attendu : [2, 3, 4, 5]
*/

const myArray = sortNumbers([4, 3, 5, 2]);


/*
  Test 2
  Résultat attendu : [-10, 2, 10, 28]
*/
const myArray1 = sortNumbers([10, 2, 28, -10]);

/* DO NOT TOUCH */
module.exports = {
  sortNumbers: sortNumbers
}
